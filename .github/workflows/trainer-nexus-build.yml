name: Build Trainer Nexus APK

on:
  push:
    branches: [trainer-nexus-build]
    paths:
      - "trainer-nexus/**"
      - ".github/workflows/trainer-nexus-build.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install API 36
        run: sdkmanager "platforms;android-36" "build-tools;35.0.0" "platform-tools"

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Build debug APK
        run: gradle -p trainer-nexus --no-daemon clean :app:assembleDebug

      - name: Verify APK signature
        run: "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs trainer-nexus/app/build/outputs/apk/debug/app-debug.apk

      - name: Stage named APK
        run: cp trainer-nexus/app/build/outputs/apk/debug/app-debug.apk Trainer-Nexus-0.1.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Trainer-Nexus-0.1
          path: Trainer-Nexus-0.1.apk
          if-no-files-found: error
          retention-days: 14
